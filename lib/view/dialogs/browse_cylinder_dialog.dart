import 'package:flutter/material.dart';

class BrowseCylinderDialog extends StatelessWidget {
  const BrowseCylinderDialog({super.key});

  @override
  Widget build(BuildContext context) {
    return Dialog(
      child: Container(),
    );
  }
}
